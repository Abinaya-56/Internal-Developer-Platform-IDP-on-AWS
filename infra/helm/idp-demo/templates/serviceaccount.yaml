{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "idp-demo.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "idp-demo.labels" . | nindent 4 }}
{{- end }}
